# 지도학습기 검증 기록

2026-09-20 05:08 KST 확인. 아래는 본학습 시작 전 검증이며 모델 성능 평가가 아니다.

- 단위·동작 테스트 22개 통과, 정적 검사·포맷 검사 통과
- 동일 조건의 CPU 연속 실행과 부분 재개 다음 업데이트 일치
- 준비 학습→미세조정 단계 전환 중단·재개와 연속 실행의 최선 모델 일치
- head만 준비 학습, 마지막 두 블록·norm·head만 미세조정되는 경계 확인
- Donate·Enes 손실 각각이 공유 인코더까지 전달됨
- 보조 가중치 0에서 Donate 단독과 동일한 업데이트
- 불균등 microbatch 누적과 전체 표본 가중 평균 손실의 업데이트 일치
- 다른 초기값·manifest로 복원 거부, 동시 학습 락 거부
- 전원 분리·저장공간 부족·중단·비정상 손실 처리 확인
- 실제 전체 특징 사전검증 재실행 통과(Donate 457, Enes 39,201)
- 실제 MPS warm-up 및 finetune 중 고정 인코더 텐서 해시 유지
- 실제 MPS 부분 체크포인트 전후 예측 동일, 재개 다음 가중치 최대 차이 `0.0`

예비 실행 코드 SHA-256:
`a399a2826c3829f60b23fd24bee53fb91f976104470d0f19c420793d90261e22`

예비 실행의 실제 저장 크기는 `best_partial_bytes=56841097`,
`resume_checkpoint_bytes=170546059`이며, 재개 체크포인트 저장은 약 0.51초였다.
표본당 검증 시간은 Donate 약 0.0207초, Enes 약 0.0110초다.

27회 실행 시간은 미세조정이 각각 5 epoch면 약 7,181초(2.0시간), 최대 20 epoch면
약 20,109초(5.6시간)로 추정됐다. 예비 실행의 두 측정 중 긴 update 시간을 사용한
시나리오다. 실제 조기 종료, 가중치 적재, 기기 발열, 다른 앱 사용에 따라 달라진다.
완료 실행의 재개 파일을 정리하면 보존 부분 가중치와 한 개 재개 상태는 약 1.64GiB다.
원본 데이터·인코더, 예비 실행 산출물과 기타 로그는 이 추정에서 별도다.

검사 환경: torch 2.14.0 / numpy 2.4.6 / timm 1.0.29 / MPS FP32.
예비 실행 시 측정한 MPS 드라이버 할당량은 약 619MiB이며 순간 스냅샷이다.
최대 프로세스 메모리 또는 시스템 전체 피크 사용량을 측정한 수치가 아니다.

전체 원본 결과는 `$RESEARCH_ROOT/runs/supervised_v1/pilot/report.json`에 보존한다.
