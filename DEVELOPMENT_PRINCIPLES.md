# 아기 울음 육아 도우미 개발 원칙

상태: 사용자 채택 완료 · 기준 계약: 1.0.0 · 적용일: 2026년 9월 19일

사용자가 “이 결정을 앞으로의 개발 과정에서의 원칙으로 삼을 수 있게 해줘”라고 요청했다. 이에 따라 두 착수 체크리스트와 당시 윤정님의 A-01~A-11을 반영한 팀 계약을 이후 기획·구현·검수·배포 준비의 기본 기준으로 적용한다. 최신 실행 순서와 역할은 업무 분담 v2.1 기준본을 함께 따른다. 이번 문서는 기존 결정의 적용 절차를 고정하며, API와 제품의 기능을 새로 변경하지 않는다.

## 기준 문서

| 목적 | 기준 |
|---|---|
| 개발 전반의 작업 방식 | 이 문서 |
| 코드 스타일·브랜치·커밋·PR·리뷰·검증·인계 | [코딩 및 협업 컨벤션](docs/project-rule/coding-conventions.md) |
| 역할·권한·상태·복구·접근 경계·시연 조건 | [팀 개발 계약](contracts/개발계약.md) |
| 실제 요청·응답·오류의 형식 | [OpenAPI 1.0.0](contracts/openapi계약.json) |
| 개발용 화면 재현 | [가상 샘플과 시험 사용자 배치](contracts/목%20응답과%20시험%20사용자%20배치.json) |
| 계약 파일의 구조 검증 기록 | [검증 결과](contracts/validation.json) |
| 제품 범위·기능·기존 인수 기준 | [PRD v2](docs/baby-care-prd-v2.docx)와 [기능명세서 v2](docs/baby-care-functional-spec-v2.docx) |
| 최신 업무 분담·구현 순서 | [A 업무 분담 v2.1 기준본](docs/baby-care-implementation-tasks-A-v2.md)·[B 업무 분담 v2.1 기준본](docs/baby-care-implementation-tasks-B-v2.md) |
| 보안 설계·시험 계획 | [보안 설계](docs/security/보안%20설계서.md)·[A·B 연결표](docs/security/AB%20보안%20작업%20연결표.md)·[62개 시험표](docs/security/62개%20보안%20시험표.md) |

업무 분담 v2.1은 기존 A-01~A-11·B-01~B-13을 보존·보강하고 A-12·A-13·B-14 및 CHAT/CHAR 인수 계획을 추가한다. 이는 구현 계획의 개정이며 API 계약 버전 변경이 아니다. 현재 OpenAPI 1.0.0에는 상담 경로가 없고, 재인증·세션 회수·아동 정보 처리·초안 수명 등 보안 문서의 신규 제안도 계약 보완 전에는 확정 필드·상태·오류로 사용하지 않는다.

팀 계약에서 명시적으로 구체화한 결정은 해당 접점의 개발 기준이다. 다른 문서와 모순이 발견되면 구현 편의에 따라 조용히 선택하지 말고 영향과 수정할 문서를 밝힌다. 사용자의 이후 명시적 변경 요청은 새 결정으로 반영한다. 계약에 맞는 일반적인 구현 선택에는 추가 승인 절차를 만들지 않는다.

사용자는 코딩 및 협업 컨벤션도 이후 작업 기준으로 채택했다. 작업 시작 시 해당 문서를 함께 읽고 구현 방식과 GitHub 협업에 적용한다. 제품 범위·기능·API 값은 기존 PRD·기능명세·계약을 유지한다. 문서 속 예시 작업이나 명령을 별도 실행 요청으로 해석하지 않는다. 이 채택 자체가 기존 코드 전체의 형식 변경, 새 기능 구현, 배포나 병합을 요청한 것은 아니다.

## 계속 유지할 원칙

1. **역할과 상태 소유권을 지킨다.** A는 웹·브라우저·캐릭터·화면 검증, B는 API·데이터·권한·ML·LLM을 담당한다. 상담에서도 A는 화면과 확인 흐름을, B는 권한 있는 조회·LLM 실행·근거·삭제를 맡는다. 서버의 상태를 화면에서 임의 생성하지 않는다. 폼·진행률·마이크 상태는 브라우저의 로컬 상태다.
2. **업무 데이터는 FastAPI 계약으로 처리한다.** Supabase 직접 연결은 Auth·승인된 private Storage 전송과 재생·비공개 변경 신호 구독만 허용한다. 브라우저 업무 Data API의 테이블·RPC 허용 목록은 비어 있다. publishable key와 사용자 access token을 사용하고 서버 비밀을 프런트엔드에 넣지 않는다.
3. **현재 권한과 아기 범위를 확인한다.** OWNER·CAREGIVER 권한, 작성자 전용 초안, 현재 DB 멤버십, 같은 baby_id 연결을 검사한다. 계정·아기 전환 때 로컬 캐시·미저장 입력·재생 URL을 정리하고 이전 응답을 새 아기에게 적용하지 않는다.
4. **한 작업을 한 번만 저장한다.** 동일한 논리 요청은 같은 Idempotency-Key를 사용한다. 같은 키의 다른 내용과 오래된 version은 409다. 응답 유실을 새 작업으로 오인하지 않는다. 이미 저장한 생활 기록을 사건의 행동에 연결할 때 집계를 늘리지 않는다.
5. **실패와 판단 유보를 구분한다.** P0 분석은 최대 45초 요청 내 실행과 60초 lease를 사용한다. COMPLETE·ABSTAIN·FAILED는 별개다. 조회 성공인 HTTP 200 안에도 저장된 FAILED 상태가 올 수 있다. 응답 유실 후 같은 analysis_id로 조회한다.
6. **초안을 사실로 만들지 않는다.** LLM 정규화는 확인 전 초안이다. 보호자 확인 때 기록·행동·상태·반응을 원자적으로 저장하고, 계획·부정·불확실한 행동을 실제 수행으로 집계하지 않는다. 원문 revision과 실행 토큰으로 오래된 결과를 차단한다.
7. **동의와 삭제의 의미를 지킨다.** 공동양육 탈퇴·개인 학습 동의 철회·자료 삭제는 별도 동작이다. 학습에는 관련 기여자의 동의와 출처가 필요하다. 삭제 중·일부 실패를 완료로 표시하지 않고, 늦은 분석·정규화가 자료를 되살리지 못하게 한다.
8. **출처와 검증 수준을 보존한다.** REAL/STUB은 실행 방식, USER/DEMO는 자료 출처다. 실제 모델 실행 여부와 모델·전처리·정규화 버전을 기록한다. 가상 자료를 성능·학습·실사용 실적으로 합치지 않는다. 보호자 보고를 객관적인 원인 정답으로 승격하지 않는다.
9. **브라우저 감지는 검증한 조건만 지원한다.** 감지 세션은 아기당 하나다. 숨김·잠금·장치 중단·통신 단절은 관측 공백이며 사용자가 명시적으로 재개한다. 전경 30분과 실제 기기 시험을 통과하기 전에는 지원 완료로 표시하지 않는다.
10. **P0와 첫 통합 시연을 구분한다.** 첫 통합 시연은 두 보호자 로그인 → 녹음 → 실제 분석 또는 정직한 유보 → 행동·상태 확인 기록 → 다른 보호자 화면 반영이다. 활성 감지·요약·학습 후보는 후속 구현 순서에 있어도 P0 완료 전에는 인수 기준을 충족해야 한다.

세부 식별자·상태값·시간 제한·허용 필드·오류 코드는 팀 계약과 OpenAPI의 현재 버전을 따른다. 이 요약만 읽고 상세 계약을 재해석하지 않는다. 내부 학습 검수·내보내기는 B 담당 범위이며, 브라우저용 OpenAPI에 없다는 이유로 P0에서 제외하지 않는다.

## 작업 시작과 완료

작업을 시작할 때 현재 폴더와 구현 상태를 확인하고 관련 F·SC·AC 또는 A/B 태스크를 연결한다. 제품·API·화면에 영향을 주는 작업은 이 원칙과 해당 계약 조항을 읽은 뒤 진행한다. 연구만 하는 작업은 출처·권리·평가·제품 연결에 해당하는 원칙을 적용하며 서비스 구현을 불필요하게 추가하지 않는다.

API가 준비되기 전에는 계약과 같은 형식의 목으로 진행한다. 윤정님은 A-01의 화면 작업과 A-02의 브라우저 오디오 선검증을 함께 시작할 수 있다. 사용자님은 인증·권한·공동 기록부터 실제 API에 연결한다. 기존 계약 안의 작업은 승인 대기로 중단하지 않는다.

완료 전에는 바뀐 범위에 맞는 검증을 수행한다. API를 바꿨으면 OpenAPI와 실제 반환값·오류·목 응답의 일치를, 권한을 바꿨으면 두 계정과 비구성원·해제 계정의 차단을, 상태를 바꿨으면 실패·재전송·늦은 응답·삭제 경로를 확인한다. 화면만 바꾼 경우에도 영향을 받는 상태 표현과 접근성을 확인한다. 관련 없는 전체 시험을 매번 반복하지 않는다.

보고할 때는 계약 작성, 구현 완료, 로컬 검증, 실제 API·RLS·브라우저·모델 검증, 배포 완료를 구분한다. 기존 `validation.json`은 계약 파일의 구조와 가상 샘플을 검증한 기록일 뿐, 실제 서비스나 모델의 통과 증거가 아니다.

## 결정을 변경하는 방법

기존 계약 안의 코드 정리·내부 구현·버그 수정은 기존 승인 범위에서 진행한다. 필드의 의미, enum, 권한, 데이터 접근 경계, 동의·삭제, 출처, P0 범위를 바꾸는 작업은 계약 변경으로 표시한다.

계약 변경을 수행할 때는 다음을 같은 작업에서 처리한다.

- 변경 이유와 전후 동작, 영향을 받는 A/B 작업 및 화면을 기록한다.
- 팀 계약·OpenAPI·목 응답·화면 매핑·관련 인수 조건을 함께 갱신한다.
- 기존 사용자의 데이터·진행 중 요청·저장된 결과에 미치는 영향을 검토한다.
- 호환성을 확인하고 계약 버전과 변경 이력을 남긴다. 형식 파괴는 major, 호환 확장은 minor, 의미 변화 없는 정정은 patch를 기본으로 하되 enum 변경도 클라이언트 처리 가능 여부를 검사한다.
- 사용자가 이미 요청하거나 승인한 변경은 재승인을 요구하지 않는다. 사용자 판단이 실제로 필요한 미결정 사항만 구체화해 확인한다.

## 다음 작업과 다른 저장소에서의 적용

현재 저장소 루트의 [AGENTS.md](AGENTS.md)가 이 문서와 계약을 읽도록 연결한다. 이후 개발과 계약 변경의 기준본은 이 저장소의 문서이며, 기존 ChatGPT 프로젝트 폴더의 문서는 이관 당시 참고본으로 보존한다. 동기화된 프로젝트 자료와 sources의 보호 규칙은 유지한다. 이미 실행 중인 다른 작업이 즉시 새 파일을 읽었다고 가정하지 않는다.

아기 울음 제품의 별도 웹·API 저장소를 만들거나 이 프로젝트를 옮길 때는 해당 저장소의 기존 AGENTS.md를 보존하면서 이 기준을 연결한다. 계약을 저장소 안으로 옮겼다면 한 위치를 기준본으로 정하고 참조 경로를 갱신한다. 복사본을 서로 독립적으로 수정하거나 개인 컴퓨터의 절대 경로만 남겨 팀원의 접근을 막지 않는다. 새 저장소의 지침 파일에서 기준 문서와 OpenAPI를 실제로 찾을 수 있는지 확인한다.

별도 작업이나 기억에서 찾은 요약은 이 기준본의 위치를 찾는 안내다. 최신 파일을 확인할 수 있으면 요약보다 현재 파일을 우선한다. 이번 요청을 근거로 아기 울음 프로젝트와 무관한 다른 프로젝트에 이 규칙을 적용하지 않는다.

## 채택 이력

| 날짜 | 기준 | 결정과 근거 |
|---|---|---|
| 2026-09-19 | 코딩 및 협업 컨벤션 | 사용자가 docs/project-rule/coding-conventions.md를 추가 컨벤션으로 채택함. 기존 개발 원칙과 함께 적용하고 저장소 지침·README에서 연결함. |
| 2026-09-19 | 저장소 작업 전환 | 사용자의 요청에 따라 EungAElien/baby-care를 이후 작업 위치로 정하고, 저장소의 기존 계약·OpenAPI·목 데이터를 기준본으로 연결함. 제품·API 계약의 내용은 변경하지 않음. |
| 2026-09-19 | 팀 계약 1.0.0 | 사용자가 두 체크리스트와 A 담당 문서에 따른 결정을 이후 개발 원칙으로 적용하도록 명시적으로 요청함. API·샘플 내용은 변경하지 않고 지침 연결과 적용 절차를 추가함. |
| 2026-09-19 | 업무 분담 v2.1 | A-12 캐릭터·A-13/B-14 개인화 상담과 보안 보강을 구현 계획에 반영함. 기존 P0·첫 통합 시연·계약 1.0.0은 유지하고 신규 상담 API와 보안 제안은 향후 계약 보완으로 구분함. |

지침 연결 방식은 [OpenAI 공식 AGENTS 지침](https://developers.openai.com/ko-KR/docs/agent-configuration/agents-md)을 2026년 9월 19일 확인했다.
