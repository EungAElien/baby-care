# A-02 ① 브라우저 오디오 기기 시험표

상태: **Mac Chrome 실기기 부분 실행, Windows Chrome 목 화면 일부 관찰, A-02 전체 미완료**. Mac의 권한 거부·허용, 사용자 중지, 앱 이탈, 로그아웃·아기 전환은 직접 확인했다. Windows Chrome 153.0.8010.48에서는 `idle → requesting → stopped`와 화면 이탈·복귀 후 `idle`만 관찰했고, 권한 프롬프트를 제어할 수 없어 실제 마이크 값은 확인하지 못했다([격리 인수 기록](./verification/a-acceptance-local-2026-09-20.md)). 장치 변경·잠금·통화/헤드셋 중단·전경 30분 및 Android/iPhone은 미확인 또는 미실행이다. 이 표는 A-02 전체, AC04~AC07, SEC25~26·33·60의 통과 증거가 아니다.

코드 검증(Mac 로컬 Worktree, 2026-09-20): 최신 `origin/develop` 통합 후 `npm ci`, `npm run typecheck`, `npm run lint`, `npm run build` 성공, `npm test` 25개 파일·117개 테스트 통과. 자동 테스트는 브라우저 오디오 API를 가짜 객체로 대체해 시작 전 권한 요청 없음, 권한 거부/API 미지원, Worklet/Recorder 값 분리, 트랙 종료·범위 전환·이탈 이벤트 정리, 늦은 권한 승인 시 트랙 폐기, 자동 재개 없음과 React Strict Mode 재설치 후 시작 가능 여부를 확인했다. 추가 회귀 시험으로 권한 요청 중 창 포커스 이탈은 중단하지 않고, 실제 `listening` 중 이탈은 중단하며, 동일 사용자·아기의 재검증은 오디오 결과를 지우지 않고 실제 범위 전환은 지우는 것을 확인했다.

## 준비와 공통 절차

1. `apps/web/`에서 `npm ci`, `NEXT_PUBLIC_ENABLE_MOCK_NAV=true`를 개발 환경에 설정한 뒤 `npm run dev`를 실행한다. 다른 기기에서는 HTTPS로 접속한다. 브라우저에서 마이크 권한이 가능한 보안 컨텍스트인지 확인한다.
2. `/login`에서 개발용 목 사용자로 아기 홈에 들어가 `/babies/{baby_id}/detect`의 **개발용 오디오 측정 화면 열기**를 선택한다. 실제 계정으로 시험할 때는 현재 아기 멤버십이 필요하다. 이 화면은 개발 빌드에서만 열린다.
3. 시작 전 브라우저의 마이크 사용 표시가 꺼져 있고 화면 값이 모두 `확인할 수 없음`인지 확인한다. **측정 시작**을 한 번 누르고 권한을 처리한다. 권한 허용 뒤 AudioWorklet 값과 MediaRecorder 결과값을 각각 기록한다. MediaRecorder 길이가 미확인이면 그대로 미확인으로 적는다.
4. 중지 또는 각 중단 상황 뒤 마이크 사용 표시가 꺼지는지, 돌아왔을 때 자동 재개하지 않는지 확인한다. 다시 시작하려면 반드시 버튼을 새로 눌러야 한다. 시험 중 음성 내용이나 Blob을 녹화·내보내기·콘솔에 기록하지 않는다.

AudioWorklet의 `첫/마지막 오디오 시계`는 벽시계가 아닌 `AudioContext` 프레임 시각이다. 처리 샘플 수는 마지막으로 수신한 Worklet 보고까지의 실제 프레임 수다. MediaRecorder 길이는 Blob의 미디어 메타데이터를 브라우저가 읽은 경우에만 표시한다. 두 값의 차이를 자동으로 성공/실패로 판정하지 않는다. 권한 표시, `track ended`, 잠금·통화 동작은 OS와 브라우저마다 달라 직접 관찰해야 한다. 브라우저 API가 잠금·통화 자체를 항상 알리지는 않는다.

## Mac Chrome 직접 관찰 (2026-09-20)

- 환경: MacBook Pro(`MacBookPro18,3`, Apple M1 Pro, 16GB), macOS 27.0(26A428), Google Chrome 154.0.8037.44 게스트 모드, MacBook Pro 내장 마이크, 로컬 개발 서버의 목 사용자.
- 권한 거부: `error`와 권한 거부 안내가 남고 Worklet/Recorder 값은 모두 `확인할 수 없음`, Chrome 마이크 표시는 꺼짐을 확인했다.
- 권한 허용·명시적 중지: 내장 마이크를 지정해 `listening`, Worklet 510,848 samples, 44,100Hz, 마지막 오디오 시계 11.619초를 관찰했다. `측정 중지` 후 `stopped / 사용자가 중지했어요.`로 바뀌고 마이크 표시가 꺼졌다. 약 3초 Recorder 결과는 48,493 bytes, `audio/webm;codecs=opus`, 길이는 `확인할 수 없음`이었다.
- 숨김·앱 이탈: 다른 앱으로 전환하자 `stopped / 화면이 숨겨져 측정을 중단했어요.`로 정리됐고, 복귀 후 자동 재개하지 않았다. 화면 잠금은 별도로 시험하지 않았다.
- 로그아웃·아기 전환: 측정 중 로그아웃 후 로그인 화면에서 마이크 표시가 꺼졌다. 아기 A에서 측정 중 아기 B로 전환한 후 정리됐고, 뒤로 돌아가도 `idle`로 남아 자동 재개하지 않았다.
- 전경 30분: 시험 중 Chrome이 전경을 잃어 오디오 시계 313.124초, 13,808,512 samples에서 이탈 정리가 실행됐다. 정리 동작은 확인했지만 30분 지속 증거는 아니므로 **미확인**으로 남긴다.
- 보호 경계: 시험 화면에 수치 메타데이터만 표시했다. 원음과 Blob을 파일·콘솔·서버로 저장하거나 전송하지 않았다.

## 기기별 기록

각 칸에 기기 모델, OS 버전, 브라우저 버전, 날짜, 결과(통과/부분 통과/실패/미확인/미실행), 재현 단계와 관측값을 남긴다.

| 기기·브라우저 | 권한 거부 → 오류·녹음 없음 | 장치 변경/트랙 종료 → 정리 | 숨김·잠금 → 정리·수동 재개 | 통화·연결 중단 → 관측 공백·수동 재개 | 전경 30분 → 샘플 증가·중지 후 정리 |
| --- | --- | --- | --- | --- | --- |
| Mac Chrome 154.0.8037.44 (macOS 27.0, MacBookPro18,3) | 통과 — `error`, 수치 미확인, 표시 꺼짐 | 미확인 — 물리 장치 변경/분리 미실행 | 부분 통과 — 탭·앱 이탈 정리/수동 재개 통과, 잠금 미실행 | 미확인 — 통화·헤드셋 분리 미실행 | 미확인 — 313.124초에 전경 이탈로 중단 |
| Windows Chrome 153.0.8010.48 (2026-09-20) | 권한 프롬프트 제어 불가로 미확인; 요청 중 중지·이탈 후 `idle` 관찰 | 미실행 | 숨김·잠금 미실행; 화면 이탈·복귀 후 자동 재개 없음 관찰 | 미실행 | 미실행 |
| Android Chrome (버전: ___) | 미실행 | 미실행 | 미실행 | 미실행 | 미실행 |
| iPhone Safari (버전: ___) | 미실행 | 미실행 | 미실행 | 미실행 | 미실행 |

### 상황별 재현

- **권한 거부:** 사이트 권한을 초기화하고 시작 버튼을 누른 뒤 거부한다. `error`와 미확인 측정값, 마이크 표시 꺼짐을 확인한다. 권한을 다시 허용해도 버튼을 다시 누르기 전에는 시작하지 않아야 한다.
- **장치 변경:** 청취 중 입력 장치를 OS/브라우저 설정에서 바꾸거나 마이크를 분리한다. 기존 트랙이 `ended`를 내는지와 중단·마이크 표시 해제를 기록한다. 브라우저가 트랙을 유지하면 그 사실과 측정값의 변화도 기록한다. 장치 변경만으로 모든 브라우저가 `ended`를 내는 것으로 가정하지 않는다.
- **숨김·잠금:** 청취 중 다른 탭/앱으로 이동하고, 별도 회차에서 화면을 잠근다. 복귀 뒤 상태·권한 표시·자동 재개 여부를 기록한다. 잠금 중 `visibilitychange`가 오지 않으면 OS 권한 표시와 샘플 증가 여부를 별도로 기록한다.
- **통화·연결 중단:** 청취 중 전화/화상 통화로 마이크를 점유하고, 별도 회차에서 헤드셋 연결을 끊는다. `ended` 또는 숨김 이벤트가 발생하는지, 발생하지 않으면 브라우저가 중단을 감지하지 못한 구간을 기록한다. 시험 뒤 사용자가 중지하고 다시 시작한다.
- **전경 30분:** 화면을 계속 켜 두고 0/10/20/30분의 Worklet 샘플 수·오디오 시계·샘플레이트를 적는다. MediaRecorder는 첫 약 3초 클립만 측정한다. 시작/끝의 마이크 표시와 중지 뒤 트랙 종료, 자동 재개 없음을 확인한다. CPU·배터리나 ONNX 성능 판정은 이 시험 범위가 아니다.
- **계정·아기 전환/로그아웃/화면 이탈:** 청취 중 아기 전환, 로그아웃, 다른 라우트 이동, 브라우저 뒤로가기를 각각 재현한다. `PrivateScope` 정리와 컴포넌트 해제 뒤 마이크 표시가 꺼지는지, 원래 화면으로 돌아와도 자동 시작하지 않는지 확인한다.

실패·미확인 상황에서는 브라우저의 오디오 측정을 지원 완료로 표시하지 않는다. 직접 녹음·파일 입력은 후속 A-05에서 제공하며, 이 시험 화면은 업로드나 분석의 대체 경로를 구현하지 않는다.
