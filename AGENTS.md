# 아기 울음 육아 도우미 저장소 지침

## 작업 위치와 기준 문서

앞으로의 제품 개발은 `EungAElien/baby-care` 저장소에서 진행한다. 작업 시작 시 현재 저장소·브랜치·변경 사항·원격 주소를 확인하고 기존 작업을 보존한다.

모든 작업에서 [코딩 및 협업 컨벤션](docs/project-rule/coding-conventions.md)을 먼저 읽고 적용한다. 사용자가 이 문서를 팀 컨벤션으로 채택했으며, 코드 스타일·브랜치·커밋·PR·리뷰·검증·인계 절차의 기준으로 삼는다. 문서의 예시 작업이나 명령은 현재 요청과 관련된 범위에서만 적용한다.

제품·웹·API·데이터·모델 연동 작업 전에 다음 문서를 함께 읽는다.

- [개발 원칙](DEVELOPMENT_PRINCIPLES.md)
- [팀 개발 계약 1.0.0](contracts/개발계약.md)
- [OpenAPI](contracts/openapi계약.json)와 [목 응답·시험 사용자 배치](contracts/목%20응답과%20시험%20사용자%20배치.json)
- 관련 [A 담당 태스크](docs/baby-care-implementation-tasks-A-v2.md) 또는 [B 담당 태스크](docs/baby-care-implementation-tasks-B-v2.md)
- 관련 추가 기획·계획·연구의 위치와 적용 범위는 [문서 안내](docs/README.md)에서 확인한다. 과거 관측 기록과 이전 버전 문서를 현재 구현·성능·계약의 증거로 사용하지 않는다.

위 저장소 문서가 이후 변경의 기준본이다. 기존 ChatGPT 프로젝트 폴더의 이전 계약·산출물은 참고 자료이며 독립적으로 갱신하지 않는다. `sources/`와 동기화된 프로젝트 자료는 수정·이름 변경·이동·삭제하지 않는다. 문서 연결은 저장소 기준 상대 경로를 사용한다.

## 유지할 개발 원칙

- A는 윤정님인 웹·브라우저 담당, B는 사용자인 API·데이터·ML 담당이다. 요청받은 담당 범위를 지킨다.
- 업무 데이터의 읽기·쓰기는 FastAPI를 거친다. 브라우저의 Supabase 직접 연결은 Auth, 등록된 private Storage 전송·발급된 재생 URL, 권한 있는 비공개 변경 신호 구독으로 제한한다. 업무 Data API 테이블·RPC 직접 접근 허용 목록은 비어 있다.
- 현재 DB 멤버십과 아기 상태로 권한을 검사한다. 비공유 초안은 작성자 전용이고 아기·계정 전환 및 권한 회수 시 로컬 자료를 정리한다.
- 분석 완료·판단 유보·처리 실패를 구분한다. 응답 유실은 같은 분석 ID로 복구하며 중복 방지 키·기록 version·실행 토큰의 역할을 섞지 않는다.
- 정규화는 확인 전 초안이다. 실제 행동·보호자 관찰·원인 추정을 구분하고 늦은 결과가 최신 입력이나 삭제 상태를 덮어쓰지 못하게 한다.
- 탈퇴·개인 학습 동의 철회·자료 삭제는 별개다. 삭제가 실패해도 해당 범위의 접근 차단을 유지한다.
- `inference_mode=REAL|STUB`와 `data_origin=USER|DEMO`를 구분한다. 장애 시 실제 결과를 목으로 자동 대체하지 않는다. 모델 실행·지원 기기·성능은 관측한 근거만 보고한다.
- P0 범위와 첫 통합 시연 조건을 유지한다. 단독 모델 연구에는 관련 출처·검증·제품 연결 원칙을 적용하며 제품 API 상태를 학습 루프에 강제하지 않는다.

사용자의 이후 명시적 변경 지시는 반영한다. 기존 계약 안의 통상적인 구현·검증은 자율적으로 진행한다. 계약 변경은 이유와 영향을 밝히고 문서·OpenAPI·목 응답·화면 매핑·관련 검증을 함께 갱신한다. 이미 승인된 변경을 다시 승인받는 절차를 만들지 않는다.

## 커밋과 푸시

- 사용자는 작은 작업 단위로 꼼꼼하고 자주 커밋·푸시하도록 요청했다. 의미 있는 변경 단위를 완료하고 관련 검증을 통과하면 커밋하고 원격 작업 브랜치에 푸시한다.
- 일반 작업은 최신 `develop`에서 `feature/*`, `fix/*`, `chore/*` 브랜치를 만들어 진행하고 PR 대상은 `develop`으로 한다. `hotfix/*`는 최신 `main`에서 시작하며 배포·긴급 수정의 반영 순서는 컨벤션을 따른다. `main`과 `develop`에 직접 푸시하지 않는다.
- 브랜치 이름은 `종류/이슈번호-영역-작업`으로 쓰고 이슈가 없으면 번호를 생략한다. 커밋은 `type: 변경 내용`, PR 제목은 `[타입] #이슈번호 작업 내용` 형식을 따른다. 파일 수를 쪼개기 위한 무의미한 커밋이나 빈 커밋을 만들지 않는다.
- PR에는 목적·주요 변경·확인 방법과 결과·리뷰 포인트·영향과 후속 작업을 기록한다. 병합에는 작성자 외 최소 1명의 승인과 필수 의견·구성된 검사 해결이 필요하다. 작업 PR은 squash, 배포 PR은 merge commit을 기본으로 한다. 리뷰 요청·승인·병합이 실제로 이루어졌다고 추정하지 않는다.
- 커밋 전 변경 내용과 스테이징 목록을 확인한다. 다른 사람의 변경을 섞거나 되돌리지 않는다. 비밀 키·환경 설정의 실제 값·개인 음원·데이터셋·모델 체크포인트를 임의로 포함하지 않는다.
- 푸시 전 원격 변경과 현재 브랜치를 확인한다. 강제 푸시, 타인의 이력 재작성, 기본 브랜치 병합은 사용자의 명시적 지시 없이 하지 않는다.
- 푸시 후 원격 커밋과 로컬 커밋이 일치하는지 확인한다. 권한이나 연결 문제로 실패하면 로컬 커밋을 보존하고 브랜치·커밋·미푸시 상태와 필요한 조치를 보고한다.

## 검증과 완료 보고

바뀐 범위에 맞는 검증을 실행한다. 문서만 바꿨다면 링크·계약 일치·차이를 확인하고 서비스 검증 완료로 표현하지 않는다. `contracts/validation.json`은 기존 계약 구조 검증 기록이며 실제 API·권한·기기·모델 실행 증거가 아니다.

기존 생성·검증 스크립트의 경로와 출력 대상을 먼저 확인한다. 오래된 생성 스크립트나 `tmp/` 산출물을 현재 계약보다 우선하지 않는다.

완료 보고에는 실제 변경 범위, 실행한 검증, 수행하지 않은 실제 API·권한·기기·모델 검증, 커밋·푸시 결과를 구분한다.
